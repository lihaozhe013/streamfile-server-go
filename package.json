{
  "name": "streamfile-server-go",
  "version": "1.0.0",
  "description": "A lightweight file server written in Go",
  "main": "index.js",
  "scripts": {
    "install:all": "npm i && cd react && npm i",
    "build": "npm run clean && npm run build-css-prod && npm run build:markdown-viewer && go build && node scripts/post-build.js",
    "build:markdown-viewer": "cd react && npm run build",
    "build:prod": "npm run build && node scripts/post-build-prod.js",
    "clean": "node scripts/clean.js",
    "build-css": "tailwindcss -i ./src/input.css -o ./public/output.css --watch",
    "build-css-prod": "tailwindcss -i ./src/input.css -o ./public/output.css --minify"
  },
  "keywords": [
    "go",
    "file-server",
    "tailwindcss"
  ],
  "author": "lihaozhe013",
  "license": "MIT",
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "fs-extra": "11.3.2"
  }
}
